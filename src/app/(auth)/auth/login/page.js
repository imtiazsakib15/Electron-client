export const metadata = {
  title: "Login | Electron",
  description: "...",
};

import Login from "@/components/Login/Login";

const LoginPage = () => {
  return (
    <>
      <Login />
    </>
  );
};

export default LoginPage;
